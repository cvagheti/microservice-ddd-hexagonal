package com.example.architecture;

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

/**
 * Suite de testes arquiteturais que agrupa todos os testes de adesão.
 * Executa validações completas da arquitetura hexagonal, DDD, CQRS e convenções.
 * 
 * Para executar todos os testes arquiteturais use:
 * mvn test -Dtest="*ArchitectureTest"
 */
@DisplayName("Testes de Adesão Arquitetural")
public class ArchitectureTestSuite {
    
    @Test
    @DisplayName("Executar todos os testes de arquitetura")
    void runAllArchitectureTests() {
        // Este método serve como placeholder
        // Use o comando Maven para executar todos os testes de arquitetura:
        // mvn test -Dtest="*ArchitectureTest"
    }
}